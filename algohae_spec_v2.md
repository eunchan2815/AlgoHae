# 알고해 (AlgoHae) — 기능 명세서 v0.3

> **한 줄 정의:** 사용자가 작성한 임의의 파이썬 코드를 브라우저에서 실제로 실행하고, 실행 한 줄 한 줄마다 변수가 어떻게 변하는지 시각적으로 보여주는 알고리즘 학습 플랫폼.

| 항목 | 내용 |
|---|---|
| 플랫폼명 | 알고해 (AlgoHae) |
| 형태 | 웹 브라우저 기반 (서버리스) |
| 1차 지원 언어 | Python (Pyodide) |
| 문서 버전 | v0.3 — 실행 안전장치·출력·직렬화 규칙 보강 |
| 변경 요약 | Web Worker 실행 구조, stdout 패널, 실행 중단, input() 정책, 스냅샷 직렬화 규칙 추가. 재귀 추적 모순 해소. 기술 스택을 React + styled-components로 확정 |

> 문서 버전(이 문서의 개정 이력)과 제품 버전(6장 로드맵)은 별개다.

---

## 1. 핵심 아키텍처: 2개의 레이어 + 실행 격리

알고해의 모든 기능은 다음 두 레이어 위에 세워진다. 이 분리가 "모든 코드를 시각화한다"는 목표를 현실적으로 달성하는 열쇠다.

### 1.1 범용 레이어 (Universal Layer)
- **목적:** 어떤 코드든 최소한의 시각화를 보장한다.
- **방식:** 파이썬의 `sys.settrace` 로 코드를 한 줄씩 추적하며, 매 줄 실행 시점의 **현재 프레임 지역 변수**를 스냅샷으로 기록한다. 추적 자체는 전 프레임(함수 내부 포함) — 재귀도 자연스럽게 동작한다.
- **결과물:** "현재 실행 줄 + 변수 테이블". 정렬·재귀·문자열·딕셔너리 등 종류를 가리지 않는다.

### 1.2 특화 레이어 (Specialized Layer)
- **목적:** 특정 자료구조를 보기 좋게 그린다.
- **방식:** 변수의 타입을 보고 전용 렌더러(React 컴포넌트)로 분기한다. 숫자 리스트는 막대, 딕셔너리는 키-값 표, 트리는 노드 그래프 등.
- **특징:** 타입 하나씩 추가하는 **플러그인 방식**. 없으면 범용 레이어로 자동 폴백된다.

### 1.3 데이터 흐름

```
사용자 코드 (모든 파이썬)
        │
        ▼
┌─ Web Worker ─────────────────────┐
│  Pyodide (Python → WASM)          │
│  범용 추적 엔진 (sys.settrace)      │
│  변수 스냅샷 — 캡처 즉시 직렬화      │
└────────────────┬─────────────────┘
                 ▼ postMessage (JSON)
        스냅샷 배열 (공통 데이터 계약)
            ┌────┴────┐
            ▼         ▼
       기본 레이어   특화 레이어
       (변수 테이블) (타입별 렌더러)
       모든 코드     선택적·점진 확장
```

핵심 원칙 두 가지:

1. **"하나의 스냅샷, 두 가지 렌더링"** — 추적 결과물(스냅샷)은 단일 포맷이고, 그것을 그리는 방법만 여러 개다.
2. **"캡처 즉시 직렬화"** — `frame.f_locals`의 리스트·딕셔너리는 참조다. 그대로 들고 있다가 실행 종료 후 읽으면 모든 스냅샷이 최종 상태로 보인다. 반드시 매 줄 캡처 시점에 직렬화(깊은 복사)한다.

### 1.4 실행 격리 (Web Worker) — 필수 구조

Pyodide는 메인 스레드가 아닌 **Web Worker 안에서** 실행한다. 이유:

- 실행 중 UI가 멈추지 않는다 (재생 컨트롤·로딩 표시가 살아 있어야 함).
- 무한루프 방어(F-10)의 스텝 상한은 **라인 이벤트 기반**이라, `sum(range(10**9))` 같은 C-레벨 장기 연산은 잡지 못한다. 이 경우 유일한 탈출구는 `worker.terminate()` — Worker 없이는 탭이 그대로 멈춘다.

---

## 2. 타깃 사용자

| 사용자 | 사용 시나리오 |
|---|---|
| 알고리즘 입문자 | 본인 코드가 한 줄씩 어떻게 도는지 추적 |
| 코딩 강사 | 수업 중 임의 코드의 변수 변화를 실시간 시연 |
| 코딩테스트 준비생 | 디버깅 — 변수가 예상과 다르게 변하는 지점 포착 |

---

## 3. 기능 명세

우선순위: **P0**(MVP 필수) / **P1**(MVP 권장) / **P2**(향후)

### 3.1 입력

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-01 | 코드 에디터 | 임의의 파이썬 코드 입력 (CodeMirror 6) | P0 |
| F-02 | 예제 라이브러리 | 정렬/재귀/딕셔너리/문자열 등 예제 | P0 |
| F-03 | 문법 강조 | 신택스 하이라이팅 | P1 |
| F-04 | 편집 ↔ 재생 모드 전환 | 실행 후 라인뷰, 편집 버튼으로 복귀 | P0 |
| F-30 | 코드 자동 저장 | localStorage — 새로고침해도 코드 유지 | P1 |
| F-32 | 랜딩 페이지 | 서비스 소개 + "코드 돌리러 가기" CTA. 다크 테마 | P0 |
| F-33 | 언어 표시 UI | 지원 언어 그리드(랜딩) — Python "시각화 지원", 나머지 "실행 지원" | P1 |
| F-34 | 멀티 파일 탐색기 | VS Code 탐색기 스타일 — 파일 생성·삭제·전환, 확장자로 언어 결정, localStorage 저장 | P1 |
| F-35 | 다국어 출력 실행 | .py 외 언어(JS/TS/Java/C++/C#/Swift/Rust/Ruby)는 실행 서버(Wandbox)로 컴파일·실행, 출력 표시. **코드가 외부 서버로 전송됨** — Python(브라우저 내)과 구분해 안내 | P1 |

### 3.2 실행 & 추적 (범용 레이어)

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-05 | Python 실행 | Pyodide(WASM)를 **Web Worker에서** 실행 | P0 |
| F-06 | 라인 단위 추적 | `sys.settrace` 로 매 줄 실행 추적. 전 프레임(함수 내부 포함) 추적 | P0 |
| F-07 | 변수 스냅샷 | 매 줄마다 현재 프레임의 지역 변수를 캡처 즉시 직렬화 | P0 |
| F-08 | 런타임 로딩 표시 | 최초 1회 런타임 다운로드 안내 | P0 |
| F-09 | 오류 표시 | 문법/런타임 오류를 읽기 쉽게 출력. 오류 줄 하이라이트 | P0 |
| F-10 | 무한루프 방어 | 3중 방어: ① 스냅샷 상한(예: 4000) ② 실행 시간 상한(예: 10초) ③ 수동 중단(F-28). C-레벨 연산은 ②·③만 유효 | P0 |
| F-27 | stdout 출력 패널 | `print` 출력을 별도 패널에 표시. 재생 스텝과 동기화 — 해당 스텝까지의 출력만 보임 | P0 |
| F-28 | 실행 중단 버튼 | 실행 중 `worker.terminate()`로 강제 종료 후 Worker 재생성 | P0 |
| F-29 | input() 안내 | MVP는 미지원 — 코드에서 감지 시 "input()은 아직 지원하지 않아요" 친절한 안내 | P0 |
| F-31 | 런타임 사전 로딩 | 페이지 진입 직후 백그라운드에서 Pyodide 다운로드 시작 (첫 실행 체감 단축) | P1 |
| F-11 | 호출 스택 시각화 | 호출 스택을 **패널로 표시** (추적 자체는 F-06에 이미 포함 — 이 항목은 UI만) | P2 |

### 3.3 시각화 — 기본 레이어

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-12 | 현재 줄 하이라이트 | 실행 중인 코드 줄 강조 | P0 |
| F-13 | 변수 테이블 | 모든 변수의 이름·타입·값 표시 | P0 |
| F-14 | 변경 변수 강조 | 직전 스텝 대비 바뀐 변수 표시 | P0 |
| F-15 | 스칼라 표현 | int/float/str/bool/None 값 표시 | P0 |

### 3.4 시각화 — 특화 레이어 (타입별 렌더러)

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-17 | 리스트 → 박스열 | 스칼라 리스트를 박스 나열로 — **변경된 원소 테두리 하이라이트** (기본 리스트 비주얼, algomaster 스타일) | P0 |
| F-16 | 리스트(숫자) → 막대 | 숫자 리스트를 막대 그래프로 (값 크기 비교용 보조 뷰) | P1 |
| F-18 | 딕셔너리 → 키-값 표 | dict를 표 형태로 | P1 |
| F-19 | 스택/큐 → 세로/가로 쌓기 | 전용 렌더러 | P2 |
| F-20 | 트리 → 노드 그래프 | 노드 배치 엔진 필요 | P2 |
| F-21 | 그래프 → 노드+엣지 | 노드 배치 엔진 필요 | P2 |

### 3.5 재생 컨트롤

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-22 | 재생·일시정지 | 스텝 시퀀스 자동 재생 | P0 |
| F-23 | 한 스텝 앞 / 뒤 | 단계 이동 | P0 |
| F-24 | 타임라인 스크럽 | 슬라이더로 임의 스텝 점프 | P0 |
| F-25 | 속도 조절 | 재생 속도(FPS) | P0 |
| F-26 | 스텝/라인 표시 | 현재 위치 표시 | P0 |

---

## 4. 데이터 모델

### 4.1 스냅샷 (범용 레이어의 출력 = 공통 계약)

```
Snapshot = {
  step  : 스텝 번호 (0부터),
  line  : 실행 중인 코드 줄 번호,
  event : "line" | "return" | "exception" | "done",
  func  : 현재 함수 이름 (모듈 레벨은 "<module>"),
  depth : 호출 깊이 (0 = 모듈 레벨) — F-11 호출 스택 UI 대비,
  vars  : { 변수명: CapturedValue, ... },   // 현재 프레임의 지역 변수
  out   : 이 스텝까지 누적된 stdout 글자 수
}
```

- 실행 종료는 `line: -1` 같은 매직값 대신 `event: "done"` 으로 표현한다.
- **stdout 동기화 기법:** 전체 stdout 문자열은 실행 종료 후 1회만 전달하고, 각 스냅샷에는 누적 길이(`out`)만 기록한다. 스텝 N에서 보여줄 출력 = `stdout.slice(0, snapshot[N].out)`. 스냅샷마다 문자열을 복사하지 않아 메모리를 아낀다.

### 4.2 CapturedValue (타입별 렌더러의 입력)

```
CapturedValue = {
  t   : "scalar" | "list" | "dict" | "opaque" | ...   (타입 태그 — 렌더러 분기 기준)
  v   : 값 (타입에 따라 형태가 다름)
  num : (리스트일 때) 전부 숫자인지 여부 → 막대 렌더 가능 판단
}
```

**설계 의도:** `t` 태그 하나만 보고 어떤 렌더러를 쓸지 결정한다. 새 자료구조를 추가한다는 건 새 `t` 값과 그에 대응하는 렌더러를 한 쌍 추가하는 것.

### 4.3 직렬화 규칙 (비기능 "직렬화" 요구사항의 구체화)

| 규칙 | 기준 (초기값 — 조정 가능) |
|---|---|
| 중첩 깊이 제한 | 3단계 초과 → `…` 축약 마커 |
| 컬렉션 길이 제한 | 100개 초과 → 앞 100개 + `…(전체 N개)` 표시 |
| 사용자 정의 클래스·미지원 타입 | `repr` 축약 (최대 120자), `t: "opaque"` |
| 순환 참조 | 축약 마커로 대체 (직렬화 실패 금지) |

---

## 5. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 성능 | 4000 스텝 이하 끊김 없는 재생 |
| UI 응답성 | 사용자 코드 실행 중에도 UI는 멈추지 않음 (Web Worker 격리) |
| 안전성 | 사용자 코드는 Pyodide 샌드박스 내에서만 실행. 어떤 코드도 강제 중단 가능 |
| 호환성 | 최신 Chrome/Edge/Safari/Firefox |
| 접근성 | 색상 외 라벨로도 상태 구분 |
| 오프라인 | 런타임 캐시 후 네트워크 없이 동작 *(서비스 워커 필요 — 제품 v0.3 이후)* |
| 직렬화 | 대용량/순환 객체는 안전하게 축약 표현 (→ 4.3) |

---

## 6. 범위 정의

### 6.1 MVP 포함 (제품 v0.1 개발 목표)
- 랜딩 페이지 → VS Code 스타일 에디터 페이지 (다크 테마)
- 임의 파이썬 코드 실행 (Web Worker + Pyodide) + `sys.settrace` 라인 추적
- 변수 테이블 (기본 레이어 전부)
- 리스트 박스열 렌더 — 변경 원소 하이라이트 (특화 레이어 1종)
- 현재 줄 하이라이트, 변경 변수 강조
- stdout 출력 패널 (스텝 동기화)
- 재생·일시정지·앞뒤·스크럽·속도
- 무한루프 3중 방어 (스텝 상한·시간 상한·중단 버튼), 오류 표시, input() 안내
- 예제 4종

### 6.2 로드맵 — 특화 렌더러를 인기 순으로 추가
| 제품 버전 | 추가 항목 |
|---|---|
| v0.2 | 딕셔너리 표, 일반 리스트 박스열, 문법 강조, 코드 자동 저장 |
| v0.3 | 스택·큐 전용 렌더러, 오프라인 지원(서비스 워커) |
| v0.4 | 함수 호출 스택 시각화 (F-11) |
| v0.5 | 트리 노드 그래프 (노드 배치 엔진) |
| v0.6 | 그래프 노드+엣지 |
| v0.7 | JavaScript 지원 — JS 인터프리터(스텝 실행 가능한 인터프리터)로 라인 추적. 2-레이어 구조 재사용 |
| v1.0 | Java·C++ 등 컴파일 언어, 코드 공유 링크, 학습 코스 모드 |

> **다중 언어 전략 (2단계):** 알고해의 핵심 가치는 "실행"이 아니라 "라인 추적 + 변수 시각화"다.
> - **1단계 — 출력 실행 (완료, F-35):** Python 외 언어는 실행 서버로 컴파일·실행해 출력만 보여준다.
>   기본은 Wandbox, Wandbox가 깨졌거나(Swift, catatonit 오류) 없는(Kotlin) 언어는 godbolt(Compiler Explorer).
>   시간 상한 90초(무한 "실행 중" 방지). (참고: Piston API는 2026-02부터 화이트리스트 전용이라 제외)
>   알려진 한계: JVM 계열(Java·Kotlin)은 서버 인코딩 문제로 한글 출력이 ?로 깨짐 — UI에서 안내.
> - **2단계 — 추적 시각화 (로드맵):** 언어별 추적 엔진 확보 순서대로 시각화 승격.
>   JS/TS(스텝 실행 인터프리터 존재, 현실적) → Java·C++ 등(장기 과제).
> - 프라이버시 원칙: Python은 브라우저 안에서만 실행(코드 미전송), 서버 실행 언어는 UI에 명시한다.

---

## 7. 기술 스택

| 레이어 | 기술 |
|---|---|
| 프런트엔드 | React 19 + TypeScript + Vite (React Compiler 활성화) |
| 스타일링 | styled-components |
| 코드 에디터 | CodeMirror 6 (문법 강조, 현재 줄 하이라이트, 읽기 전용 전환) |
| 실행 | Pyodide (Python → WebAssembly), **Web Worker 내 실행** |
| 추적 | `sys.settrace` (라인 이벤트 + frame.f_locals 캡처 즉시 직렬화) |
| 통신 | Worker → 메인: `postMessage` 로 JSON 스냅샷 배열 전달 |
| 배포 | 정적 호스팅 (서버 불필요) |

---

## 8. 결정 사항 (구 "열린 설계 질문"에 대한 결론)

1. **함수 추적 깊이** → 전 프레임 추적 + 표시는 현재 프레임 지역변수. 이로써 재귀 예제(F-02, P0)가 MVP에서 성립한다. 호출 스택 **패널**만 P2(F-11)로 분리.
2. **객체 표현** → MVP는 `repr` 축약(120자, `t: "opaque"`). 속성 펼치기는 P2.
3. **추적 비용** → 3중 방어로 해결: 스텝 상한 4000 + 시간 상한 + 수동 중단(terminate). `sys.settrace`의 감속은 상한 안에서는 문제 없음.
4. **특화 렌더러 등록 방식** → `Record<TypeTag, React.ComponentType<RendererProps>>` 매핑 객체. 키가 없으면 기본 변수 테이블로 폴백. 새 렌더러 추가 = 맵에 한 줄 추가.

## 9. 남은 열린 질문

1. **input() 장기 정책:** 계속 미지원으로 갈지, stdin 사전 입력란(실행 전에 입력값을 미리 적어두는 방식)을 P2로 넣을지?
2. **시간 상한 기준값:** Pyodide는 네이티브 대비 수~수십 배 느림. 5초? 10초? — 실측 후 결정.
3. **스텝 상한 4000의 근거:** 직렬화 비용(스냅샷당 변수 크기)에 따라 재조정 여지. 실측 후 결정.
